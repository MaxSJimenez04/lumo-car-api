$kind: http-request
name: actualizar admin-sucursal :usuario
description: Update the branch assigned to a branch admin. Bearer auth required.
method: PUT
url: "{{baseUrl}}/usuarios/:usuario/admin-sucursal"
examples: .resources/admin-sucursal-actualizar.resources/examples
order: 8000
headers:
  Content-Type: application/json
pathVariables:
  usuario: BrendaMarquez
body:
  type: json
  content: |-
    {
      "idSucursal": 2
    }
