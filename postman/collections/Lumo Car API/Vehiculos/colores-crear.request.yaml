$kind: http-request
name: crear color
description: Create a color. Bearer auth required.
method: POST
url: "{{baseUrl}}/vehiculos/colores"
examples: .resources/colores-crear.resources/examples
order: 2000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "color": "Rojo",
      "codigoHex": "#FF0000"
    }
