$kind: http-request
name: listar vehiculos por sucursal
description: List vehicles by branch. No Auth (public).
method: GET
url: "{{baseUrl}}/vehiculos?idSucursal=1"
queryParams:
  idSucursal: "1"
examples: .resources/listar.resources/examples
order: 4000
auth:
  type: noauth
headers:
  Content-Type: application/json
